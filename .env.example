# Optional: Override CLI binary paths (auto-detected by default)
# Use these if CLIs are not in your PATH or you want to use specific versions
# CLAUDE_CLI_PATH=/custom/path/to/claude
# CODEX_CLI_PATH=/custom/path/to/codex

# Optional: API token authentication for Claude (OAuth via CLI is default)
# Priority: CLAUDE_CODE_OAUTH_TOKEN > ANTHROPIC_API_KEY > CLI OAuth
# ANTHROPIC_API_KEY=sk-ant-...
# CLAUDE_CODE_OAUTH_TOKEN=...

# Optional: Codex API key (OAuth via 'codex login' is default)
# CODEX_API_KEY=...

# Optional: Enable E2E tests with real CLIs (disabled by default for speed)
# RUN_E2E_TESTS=true
